# Deployment Configuration

# ============ Factory Deployment (one-time) ============

# Admin/multisig that can upgrade all DelegatedAccount instances via the beacon
BEACON_OWNER=0x...

# ============ Create DelegatedAccount ============

# Factory address (from DeployFactoryScript output)
FACTORY=0x...

# Owner address (MM's main wallet - can withdraw funds)
OWNER=0x...

# Operator address (hot wallet - can trade but not withdraw)
OPERATOR=0x...

# Perpl Exchange contract address (collateral token is fetched from it automatically)
EXCHANGE=0x9c216d1ab3e0407b3d6f1d5e9effe6d01c326ab7

# ============ Setup DelegatedAccount ============

# DelegatedAccount proxy address (from CreateAccountScript output)
DELEGATED_ACCOUNT=0x...

# Initial deposit amount for createAccount (raw token units, e.g. 100_000_000 for 100 USDC)
DEPOSIT_AMOUNT=100000000

# Enable order forwarding (optional, defaults to false)
# ENABLE_FORWARDING=true

# ============ Upgrade ============

# Beacon address (from factory.beacon() or DeployFactoryScript output)
BEACON=0x...

# New implementation address (from DeployImplementationScript output)
NEW_IMPLEMENTATION=0x...

# ============ Common ============

# Deployer private key
PRIVATE_KEY=0x...

# RPC URL
RPC_URL=https://monad-testnet.drpc.org
